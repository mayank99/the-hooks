{
	"name": "the-hooks-monorepo",
	"private": true,
	"version": "0.0.0",
	"workspaces": [
		"packages/*",
		"apps/*"
	],
	"scripts": {
		"build": "turbo run build",
		"dev": "turbo run dev"
	},
	"devDependencies": {
		"tsup": "^7.2.0",
		"turbo": "latest",
		"typescript": "^5.1.6"
	}
}
