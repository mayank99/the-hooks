{
	"name": "@the-hooks/use-uncontrolled-state",
	"version": "0.1.0",
	"description": "A wrapper around useState that gives preference to the uncontrolled prop value",
	"type": "module",
	"module": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": [
		"index.ts"
	],
	"repository": {
		"type": "git",
		"url": "github.com/mayank99/the-hooks/packages/use-uncontrolled-state"
	},
	"author": "mayank99",
	"license": "MIT",
	"scripts": {
		"build": "tsup index.ts --format esm --dts",
		"dev": "tsup index.ts --format esm --watch --dts"
	},
	"dependencies": {},
	"peerDependencies": {
		"react": ">=16.8.0"
	},
	"devDependencies": {
		"react": "^18.1.0",
		"@types/react": "^18.0.0"
	}
}
